var {{pascalcase name}} = require('../lib/component');
var assert = require('assert');

describe('{{pascalcase name}}', function() {
  it('should work', function() {
    assert.ok({{pascalcase name}});
  });
});
